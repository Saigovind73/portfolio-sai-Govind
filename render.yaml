services:
  - type: web
    name: portfolio-sai-govind
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node api/index.js
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
